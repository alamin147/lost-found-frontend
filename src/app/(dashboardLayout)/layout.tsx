import { ReactNode } from "react";

const Layout = ({ children }: { children: ReactNode }) => {
  return (
    <>
    <div>dsfdsfs</div>
      <div>{children}</div>
    </>
  );
};
export default Layout;
